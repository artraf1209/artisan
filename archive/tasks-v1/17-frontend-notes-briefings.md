# Task 17 — Frontend: Notes + Briefings Pages

**Status:** ⬜ pending  
**Depends on:** 13 14 16  
**Estimated effort:** 2–4 hours  

---

## Goal
Add pages to view per-signal thesis notes and the daily briefing history generated by the Python hybrid engine.

---

## Files to create

```text
app/src/app/trades/notes/[id]/page.tsx
app/src/app/briefings/page.tsx
app/src/components/trades/ThesisNote.tsx
app/src/components/briefings/BriefingList.tsx
app/src/components/briefings/BriefingCard.tsx
app/src/app/api/briefings/route.ts
```

---

## Implementation notes

- `/trades/notes/[id]` should load one signal and its related thesis analysis.
- `/briefings` should list recent `analysis_type='briefing'` rows in reverse chronological order.
- Reuse the hybrid types from Task 16.
- For notes pages, show:
  - symbol
  - score summary
  - status
  - thesis content
  - review note if present
- For briefings, show:
  - created time
  - model name
  - rendered briefing text

---

## Acceptance criteria

- [ ] Signal thesis notes are viewable by signal id
- [ ] Recent briefings render in a dedicated `/briefings` page
- [ ] Pages handle missing analyses gracefully
- [ ] UI reads directly from the new hybrid tables
